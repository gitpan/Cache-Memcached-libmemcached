TYPEMAP
Cache_LibMemcached *            T_CACHE_LIBMEMCACHED
Cache_LibMemcached_rc           T_CACHE_LIBMEMCACHED_RC

INPUT
T_CACHE_LIBMEMCACHED
        $var = XS_STATE(Cache_LibMemcached *, $arg);

OUTPUT
T_CACHE_LIBMEMCACHED
        if ($var == NULL) {
            sv_setsv($arg, &PL_sv_undef);
        } else {
            XS_STRUCT2OBJ($arg, "Cache::Memcached::LibMemcached::Backend", $var);
        }

T_CACHE_LIBMEMCACHED_RC
        $arg = newSViv($var);

